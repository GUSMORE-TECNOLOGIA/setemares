{
  "version": 2,
  "buildCommand": "cd desktop && npm run build",
  "outputDirectory": "desktop/dist",
  "framework": "vite",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
